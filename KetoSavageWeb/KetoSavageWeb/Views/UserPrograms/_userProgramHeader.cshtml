@model KetoSavageWeb.ViewModels.UserProgramViewModel

@using (Html.BeginForm("UserProgramHeader", "UserPrograms", FormMethod.Post, new { id = "userProgramHeaderForm", @class = "edit_form" }))
{
    Html.DevExpress().FormLayout(header =>
    {
        header.Name = "userProgramHeader";
        header.SettingsItemCaptions.Location = LayoutItemCaptionLocation.Top;
        header.Width = Unit.Percentage(100);
        header.Height = Unit.Pixel(400);


        header.Items.AddGroupItem(g =>
        {
            g.ColCount = 2;
            g.ColSpan = 1;
            g.GroupBoxDecoration = GroupBoxDecoration.Box;
            g.ShowCaption = DefaultBoolean.True;
            g.Caption = "Program Details";
            g.Height = Unit.Pixel(100);


            g.Items.Add(m => m.UserName, i =>
            {
                i.Caption = "User Name";
                i.FieldName = "UserName";
                i.NestedExtensionType = FormLayoutNestedExtensionItemType.TextBox;
            });
        });
        //header.Items.AddGroupItem(g =>
        //{
        //    g.ColCount = 2;
        //    g.ColSpan = 1;
        //    g.GroupBoxDecoration = GroupBoxDecoration.Box;
        //    g.ShowCaption = DefaultBoolean.True;
        //    g.Caption = "Daily Macros";
        //    g.Items.Add(i =>
        //    {
        //        i.SetNestedContent(() =>
        //        {
        //            ViewContext.Writer.Write("Daily macros for the past week and current week");
        //        });
        //    });
        //});
        //header.Items.AddGroupItem(g =>
        //{
        //    g.ColCount = 2;
        //    g.ColSpan = 1;
        //    g.GroupBoxDecoration = GroupBoxDecoration.Box;
        //    g.ShowCaption = DefaultBoolean.True;
        //    g.Caption = "Update Macros";
        //    g.Items.Add(i =>
        //    {
        //        i.SetNestedContent(() =>
        //        {
        //            ViewContext.Writer.Write("Set the new week's macros here");
        //        });
        //    });
        //});
    }).Render();
}