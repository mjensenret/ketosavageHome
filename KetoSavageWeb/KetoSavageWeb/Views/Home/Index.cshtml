
@{
    ViewBag.Title = "Index";
}
@Html.DevExpress().Panel(p =>
{
    p.Name = "mainContentPanel";
    p.ControlStyle.BackColor = System.Drawing.Color.IndianRed;
    p.SetContent(() =>
    {
        if(Request.IsAuthenticated)
        {
            //Html.RenderPartial("_mainMenuButtons");
            //if(ViewBag.IsCoachOrAdmin)
            //{
            //    Html.RenderAction("mainMenuButtons", "admin");
            //}
            //else
            //{
            //    Html.RenderAction("mainMenuButtons", "user");
            //}

            //ViewContext.Writer.Write("What happens if I put this panel in here? - Home/Index page");
        }
        else
        {
            ViewContext.Writer.Write("Not logged in - Home/Index page");
            Html.RenderPartial("_userLoginForm");
        }
    });
}).GetHtml()


